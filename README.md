# Проект "Интернет-магазин"

## Описание
В проекте производится разработка ядра для интернет-магазина в рамках темы E-commerce  — электронная торговля, или электронная коммерция. В дальнейшем для этого ядра возможно будет реализовать любой интерфейс — от сайта до телеграм-бота.

## Создание классов
В проекте создано несколько классов для приложения:

- **В модуле category** создан:
  - Класс — `Category`.

### Определение свойств
- **Для класса `Category`** определите следующие свойства: название (name), описание (description), список товаров категории (products).

### Атрибуты класса
- **Для класса `Category`** добавлены два атрибута класса: количество категорий (category_count) и количество товаров (product_count). Атрибуты класса заполняются автоматически при инициализации нового объекта.
- ** Для класса `Category`** доработаны  ограничения сложения:  функциональность сложения  доработана таким образом, чтобы можно было складывать товары только из одинаковых классов продуктов.
- ** Для класса `Category`** доработаны ограничения добавления продукта: доработан метод, который добавляет продукт в категорию таким образом, чтобы не было возможности добавить вместо продукта или его наследников любой другой объект.

- **В модуле category_iterator**создан новый вспомогательный класс, с помощью которого можно перебирать товары одной категории:
  - Класс — `CategoryIterator`. Новый класс принимает на вход объект класса категории и производит итерацию по товарам, которые хранятся в данной категории.

- **В модуле product** создан:
  - Класс — `Product`.

### Определение свойств
- **Для класса `Product`** определены следующие свойства: название (name), описание (description), цена (price), количество в наличии (quantity).

## Создание классов - наследников
В проекте создано несколько классов  - наследников класса Product:

- **В модуле lawngrass**создан:
  - Класс — `LawnGrass`.

- **В модуле smaryphone**создан:
  - Класс — `Smaryphone`.

### Определение свойств
- **Для класса `LawnGrass`** дополнительно определены следующие свойства: страна-производитель (country), срок прорастания (germination_period), цвет (color).
- **Для класса `Smaryphone`** дополнительно определены следующие свойства: производительность (efficiency), модель (model), объем встроенной памяти (memory), цвет (color).

## Создание базового класса
В проекте создан базовый абстактный класс, который является родительским для класса продуктов:

- **В модуле baseproduct** создан:
  - Базовый абстрактный класс — `BaseProduct`.

### Функциональность
- **Классы `Smaryphone` и `LawnGrass`** остаются наследниками класса Product и тем самым наследуют все свойства абстрактного класса. 
- **В классе `BaseProduct`** описана общая функциональность, которая должна быть у каждого продукта.

## Реализация класа-миксин
В проекте реализован класс-миксин, который является родительским для класса продуктов:

- **В модуле `logging_mixin** создан:
  - Класс — `LoggingMixin`.

### Функциональность
- **Класс `LoggingMixin`** при создании объекта, то есть при работе метода __init__, печатает в консоль информацию о том, от какого класса и с какими параметрами был создан объект.
- **Класс `LoggingMixi`** добавлен в цепочку наследования класса `Product`.

## Создание класса Заказ
В проекте создан класс Заказ:

- **В модуле `order** создан:
  - Класс — `Order`.

- **В модуле `base_order_category** создан:
  - Класс — `BaseOrderCategory`.

### Функциональность
- **Класс `Order`** формирует ссылку на то, какой товар был куплен, количество купленного товара, а также итоговая стоимость. В заказе может быть указан только один товар.
- **В общем абстактном классе `BaseOrderCategory`** выделены общие свойства классов `Order` и `Category`.  

## Реализация функций
В проекте реализовано несколько функций для приложения:

- **В модуле utils** реализованы:
  - Функция чтения файла JSON — `read_json`.
  - Функция для создания обьектов классов — `create_objects_from_json`.

### Как работает функция
- **Функция `read_json`** читает JSON файл и возвращает данные в виде словаря.
- **Функция `create_objects_from_json`** создает объекты категорий и продуктов из данных JSON.

## Установка
1. Клонирование репозитория
```git clone https://github.com/Vitaly-Shcheglov/The-Online-Store-Project.git```

2. Запуск скрипта для демонстрации возможностей

## Тестирование

В ходе тестирования кода был выполнен ряд проверок и отладки отдельных модулей:

### Модуль category

#### Функция test_category_initialization:
- **Тестирует** инициализацию объекта Category.

#### Функция test_product_count_in_category:
- **Тестирует** подсчет количества продуктов в категории.

#### Функция test_category_count:
- **Тестирует** подсчет общего количества категорий.

#### Функция test_category_str:
- **Тестирует** строковое представление категории.

#### Функция test_add_invalid_product:
- **Тестирует** попытку добавления некорректного объекта в категорию.

### Модуль category_iterator

#### Функция test_category_iterator:
- **Тестирует** функциональность итератора для категории.

#### Функция test_category_iterator_empty:
- **Тестирует** итератор для пустой категории.

### Модуль product

#### Функция test_product_initialization:
- **Тестирует** инициализацию объекта Product.

#### Функция test_product_price_setter:
- **Тестирует** установку цены продукта.

#### Функция test_product_price_setter_negative:
- **Тестирует** установку отрицательной цены.

#### Функция test_product_price_setter_zero:
- **Тестирует** установку нулевой цены.

#### Функция test_product_new_product:
- **Тестирует** оздание нового продукта с данными из словаря.

#### Функция test_product_new_product_existing:
- **Тестирует** создание нового продукта, если продукт с таким же именем уже существует.

#### Функция test_product_new_product_existing_no_update:
- **Тестирует** создание нового продукта без обновления, если цена ниже существующей.

#### Функция test_product_str:
- **Тестирует** строковое представление продукта.

#### Функция test_product_addition:
- **Тестирует** сложение двух продуктов.

### Модуль utils

#### Функция test_read_json:
- **Тестирует функцию read_json.** Проверяет, что функция корректно считывает данные из имитированногоJSON файла и возвращает их в виде словаря.

#### Функция test_create_objects_from_json:
- **Тестирует функцию test_create_objects_from_json.** Проверяет, что функция корректно создает объекты категорий и продуктов из данных JSON.

### Модуль test_lawngrass:

#### Функция test_lawn_grass_initialization:
- **Тестирует инициализацию объекта LawnGrass.**

#### Функция test_lawn_grass_str:
- **Тестирует строковое представление газонной травы.**

#### Функция test_lawn_grass_inheritance:
- **Тестирует, что LawnGrass** наследует свойства от Product.

### Модуль test_smartphone:

#### Функция test_smartphone_initialization:
- **Тестирует инициализацию объекта Smartphone.**

#### Функция test_smartphone_str:
- **Тестирует строковое представление смартфона.**

#### Функция test_smartphone_inheritance:
- **Тестирует, что Smartphone** наследует свойства от Product.

### Модуль test_baseproduct:

#### Функция test_product_initialization:
- **Тестирует инициализацию продукта.**

#### Функция test_product_str:
- **Тестирует что строковое представление продукта.**

#### Функция test_product_addition:
- **Тестирует сложение двух продуктов.**

#### Функция test_smartphone_initialization:
- **Тестирует инициализацию смартфона.**

#### Функция test_lawngrass_initialization:
- **Тестирует инициализацию газонной травы.**

### Модуль test_logging_mixin:

#### Функция test_logging_on_creation:
- **Тестирует логирование при создании объекта.**

#### Функция test_logging_repr:
- **Тестирует строковое представление объекта.**

#### Функция test_product_initialization:
- **Тестирует инициализацию объекта Product.**

#### Функция test_product_price_setter:
- **Тестирует установку цены продукта.**

#### Функция test_product_price_setter_negative:
- **Тестирует установку отрицательной цены.**

#### Функция test_product_price_setter_zero:
- **Тестирует установку нулевой цены.**

#### Функция test_product_addition:
- **Тестирует сложение двух продуктов.**

### Модуль test_order:

#### Функция test_order_initialization:
- **Тестирует тестирует инициализацию объекта Order.**

#### Функция test_order_details:
- **Тестирует тестирует метод details** для заказа.

#### Функция test_order_str:
- **Тестирует строковое представление объекта Order.**
